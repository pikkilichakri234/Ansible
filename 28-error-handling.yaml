- name: Error Handling
  hosts: all
  become: yess
  tasks:
    - name: Fail this command
      ansible.builtin.shell:
        cmd: exit 0
      ignore_errors: true

    - name: Print Hello
      ansible.builtin.debug:
        msg: "Hello, om namaha shivaya"
