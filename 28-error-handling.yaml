- name: Error Handling
  hosts: localhost
  connection: local
  tasks:
    - name: Fail this command
      ansible.builtin.shell:
        cmd: exit 1
      ignore_errors: true

    - name: Print Hello
      ansible.builtin.debug:
        msg: "Hello"
